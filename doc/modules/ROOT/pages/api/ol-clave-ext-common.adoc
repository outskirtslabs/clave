= ol.clave.ext.common


Common utilities for clave server extensions.

This namespace provides server-agnostic helpers for working with
clave's automation layer, including keystore creation and event processing.

These functions can be used by any server extension (Jetty, http-kit, etc.).

[#create-keystore]
== create-keystore




[source,clojure]
----
(create-keystore bundle)
(create-keystore bundle password)
----

Create an in-memory PKCS12 KeyStore from a clave certificate bundle.

No disk I/O - purely in-memory operation suitable for TLS handshakes.

[options="header"]
|===
| key | description
| `bundle` | Certificate bundle from xref:api/ol-clave-automation.adoc#lookup-cert[`ol.clave.automation/lookup-cert`]
| `password` | Optional keystore password (default "changeit")
|===

Returns a `java.security.KeyStore` ready for use with TLS servers.
Returns nil if bundle is nil (no certificate available yet).

[source,clojure]
----
(create-keystore (auto/lookup-cert system "example.com"))
;; => #object[java.security.KeyStore ...]
----

[.api-source]
link:https://github.com/outskirtslabs/clave/blob/main/src/ol/clave/ext/common.clj#L16-L45[source,window=_blank]

'''

[#certificate-event-QMARK-]
== certificate-event?




[source,clojure]
----
(certificate-event? evt)
----

Check if an event indicates a certificate change.

Returns true for `:certificate-obtained` and `:certificate-renewed` events.

[options="header"]
|===
| key | description
| `evt` | Event from xref:api/ol-clave-automation.adoc#subscribe-events[`ol.clave.automation/subscribe-events`]
|===

[source,clojure]
----
(when (certificate-event? evt)
  (log/info "Certificate updated for" (event-domain evt)))
----

[.api-source]
link:https://github.com/outskirtslabs/clave/blob/main/src/ol/clave/ext/common.clj#L47-L62[source,window=_blank]

'''

[#event-domain]
== event-domain




[source,clojure]
----
(event-domain evt)
----

Extract the domain name from a certificate event.

Returns the domain string or nil if event has no domain.

[options="header"]
|===
| key | description
| `evt` | Event map
|===

[.api-source]
link:https://github.com/outskirtslabs/clave/blob/main/src/ol/clave/ext/common.clj#L64-L73[source,window=_blank]

'''

[#wrap-redirect-https]
== wrap-redirect-https




[source,clojure]
----
(wrap-redirect-https handler)
(wrap-redirect-https handler {:keys [ssl-port] :or {ssl-port 443}})
----

Ring middleware that redirects HTTP requests to HTTPS.

[options="header"]
|===
| key | description
| `handler` | Ring handler to wrap
| `opts` | Options map with `:ssl-port`
|===

Options:
- `:ssl-port` - HTTPS port for redirect URL.
  Defaults to 443 (implicit, no port in URL).
  Use a custom port like 8443 to include it explicitly.

Passes through requests that are already HTTPS (by `:scheme` or `x-forwarded-proto` header).

[source,clojure]
----
(wrap-redirect-https handler {:ssl-port 8443})
----

[.api-source]
link:https://github.com/outskirtslabs/clave/blob/main/src/ol/clave/ext/common.clj#L75-L106[source,window=_blank]

'''

[#no-op-solver]
== no-op-solver




[source,clojure]
----
(no-op-solver)
----

Create a no-op ACME solver for testing.

Returns a solver that does nothing.
Useful with `PEBBLE_VA_ALWAYS_VALID=1` where challenge validation is skipped.

[source,clojure]
----
{:solvers {:http-01 (no-op-solver)}}
----

[.api-source]
link:https://github.com/outskirtslabs/clave/blob/main/src/ol/clave/ext/common.clj#L108-L119[source,window=_blank]

'''

[#missing-certificates]
== missing-certificates




[source,clojure]
----
(missing-certificates system domains)
----

Returns domains without a currently available certificate.

Uses xref:api/ol-clave-automation.adoc#lookup-cert[`ol.clave.automation/lookup-cert`] as the authoritative state source.

[options="header"]
|===
| key | description
| `system` | Clave automation system
| `domains` | Domains to check
|===

[.api-source]
link:https://github.com/outskirtslabs/clave/blob/main/src/ol/clave/ext/common.clj#L121-L131[source,window=_blank]

'''

[#wait-for-certificates]
== wait-for-certificates




[source,clojure]
----
(wait-for-certificates system domains event-queue timeout-ms poll-interval-ms)
----

Waits for certificates to be available for all `domains`.

Certificate state from xref:api/ol-clave-automation.adoc#lookup-cert[`ol.clave.automation/lookup-cert`] is authoritative.
The function accepts an existing `event-queue`, an optional `timeout-ms`, and
a positive `poll-interval-ms`.
It reports a terminal `:certificate-failed` event for a still-missing domain
immediately and reports `:subscription-overflow` instead of degrading to a
timeout.

Create `event-queue` before xref:api/ol-clave-automation.adoc#manage-domains[`ol.clave.automation/manage-domains`] because
subscriptions receive only live events.
The caller must pass the queue to
xref:api/ol-clave-automation.adoc#unsubscribe-events[`ol.clave.automation/unsubscribe-events`] in a `finally` clause.

[options="header"]
|===
| key | description
| `system` | Clave automation system
| `domains` | Domains to wait for
| `event-queue` | Queue from xref:api/ol-clave-automation.adoc#subscribe-events[`ol.clave.automation/subscribe-events`]
| `timeout-ms` | Timeout in milliseconds, or `nil` for none
| `poll-interval-ms` | Maximum interval between authoritative checks
|===

Returns `nil` once every certificate is available.

[source,clojure]
----
(let [events (auto/subscribe-events system)]
  (try
    (auto/manage-domains system ["example.com"])
    (wait-for-certificates system ["example.com"] events 120000 100)
    (finally
      (auto/unsubscribe-events system events))))
----

[.api-source]
link:https://github.com/outskirtslabs/clave/blob/main/src/ol/clave/ext/common.clj#L133-L211[source,window=_blank]
