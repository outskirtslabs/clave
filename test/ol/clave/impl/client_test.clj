(ns ol.clave.impl.client-test
  (:require
   [clojure.test :refer [deftest is testing]]))

;; Tests moved to ol.clave.impl.account-test
;; - create-normalizes-contact
;; - create-rejects-invalid-contact  
;; - create-validates-account-spec
;; - generate-keypair-produces-keypair
;; - deserialize-test-fixture
